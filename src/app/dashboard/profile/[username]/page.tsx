'use client';
import { ProfilePageClient } from '@/components/profile-page-client';

// The page renders the client component which fetches and displays profile data.
export default function ProfilePage() {
  return <ProfilePageClient />;
}
